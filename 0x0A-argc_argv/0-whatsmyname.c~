#include <stdio.h>
#include "main.h"

/**
 *main - main function
 *@argc - argument count
 *@argv - array of arguments
 *Return - 0 if success
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
