#include <stdio.h>
#include "main.h"

/**
 *_atoi - takes string to character
 *@s: character to be converted
 *Return: 1 if the letter is lowercase or uppercase. 0 otherwise
 */

int _atoi(char *s)
{
	return (0);
}
