#include <stdio.h>
#include "main.h"

/**
 *_islower - checks if argument passed is alphabet
 *@c: The character to be checked
 *
 *Return: 1 if the letter is lowercase or uppercase. 0 otherwise
 */

int _islower(int c)
{
	return (0);
}
