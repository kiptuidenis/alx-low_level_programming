#include <stdio.h>
#include "main.h"

/**
 *_isalpha - function
 *@c: character to be checked
 *Return: 1 if the letter is lowercase or uppercase. 0 otherwise
 */

int _isalpha(int c)
{
	return (0);
}
