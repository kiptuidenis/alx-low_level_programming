#include <stdio.h>
#include "main.h"

/**
 *_isdigit - concantenates two strings
 *@c: digit to be checked
 *Return: 1 if the letter is lowercase or uppercase. 0 otherwise
 */

int _isdigit(int c)
{
	return (0);
}
