#include <stdio.h>
#include "main.h"

/**
 *_strspn - function
 *@s: string 1
 *@accept: string 2
 *Return: 1 if the letter is lowercase or uppercase. 0 otherwise
 */

unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
